# apps/core

Core service scaffold placeholder.

Planned modules:

- `api`: REST + WebSocket endpoints
- `fleet`: node/service state ingest and query
- `alerts`: rule evaluation and alert lifecycle
- `audit`: action audit trail
- `plugins`: plugin manifest loading and validation

